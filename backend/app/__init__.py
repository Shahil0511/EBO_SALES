"""LIBAS Sales Intelligence — backend application package.

A directory becomes an importable Python *package* only when it contains an
`__init__.py`. That is what lets us write `from app.main import app` and what makes
`uvicorn app.main:app` resolve. (Python's rough equivalent of an `index.js` barrel
that marks a folder as a module.) We keep it intentionally empty.
"""
